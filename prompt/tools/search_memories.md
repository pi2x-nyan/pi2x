---
label: 查询记忆
---

查询 bot 记忆库中的历史事实。支持按关键词、时间范围、来源、类型过滤。可以回忆用户曾经告诉过的偏好/约定/事件，或检索之前对话留下的记录。

**使用规范**：
- 一次搜索拿到结果后，**直接用结果即可，不要重复调用相同搜索**（同一会话同一关键词只搜一次）。
- 若消息中已自动注入【记忆】块且已含所需信息，直接使用，**不必再搜索**。
- 只有在需要更精确/更新/特定来源的结果时，才用不同参数再搜一次。