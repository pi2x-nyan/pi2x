---
label: QQ 主动发消息
---

主动向当前对话的 QQ 会话发送一条消息（群聊发给该群，私聊发给该用户）。

**进度/汇报通道**——必须用它向用户实时汇报：
- 收到用户消息后，**第一轮面向用户的输出**就要先调用本工具回复用户（哪怕只是"好，我开始处理了"）。不要一上来就埋头调工具、让用户等。
- 之后**每 5 个实质输出轮**（向用户输出的轮次；连续调工具不计入）调用本工具汇报一次进展。
- 最终再给完整汇总。不要把多步进度全部攒在最后一条回复里。
