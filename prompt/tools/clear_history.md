# clear_history — 清除自己的聊天历史

清除**当前用户自己**的聊天记录（私聊会话 + 该用户在任意群里的分会话；含运行内存中的会话缓存）。

- **仅能清除自己**（按当前会话用户强制判定，无法清除他人）
- 权限：operator 及以上（friend/dialog 无该 token，会被拒绝）
- 不删除群共享会话文件（`group_<群号>.jsonl`，那里面可能含他人的会话）
- 适用场景：用户要求"清除/删除我的聊天记录/历史"时主动调用
- 参数：无（scope 固定为自己）
- 执行后该用户所有会话从零开始（新消息会新建会话）